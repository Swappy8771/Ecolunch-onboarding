/** UI-facing type for the admin-picker use case — Users has no dedicated admin page yet, so this is the only ViewModel this feature needs so far. */
export interface UserOptionViewModel {
  id: string
  fullName: string
  email: string
}
