/**
 * DTO → ViewModel mapping boundary for Contracts — infrastructure
 * placeholder only (Phase 2, Typed API Contract Layer). Real mappers
 * (e.g. `ContractDTO` → a `ContractViewModel` a card component actually
 * renders) are implemented during this module's own Phase 2 integration —
 * see `knowledge/03-frontend/phase-2-api-integration/Contracts.md` — once that
 * module's `types/` are hand-mirrored from the real backend
 * `contracts.dto.ts` and a ViewModel shape is actually needed by a real
 * component. Not implemented here, per this phase's explicit "create the
 * infrastructure only, do not implement every mapper" instruction.
 */
export {}
